## 1. Contract

- [x] 1.1 Update OpenSpec requirements for a template-shipped operator-local xUnit contour, generated runtime truth, and direct-platform profile fields
- [x] 1.2 Document the canonical TDD loop and fail-closed limits for delete/rename changes

## 2. Template Surface

- [x] 2.1 Add template-managed xUnit assets: direct-platform runner, EPF build helper, generic server-side harness, starter config, and local TDD wrapper
- [x] 2.2 Wire direct-platform example profiles and generated-project starter/runtime truth to the shipped xUnit contour while keeping unsupported adapters fail-closed
- [x] 2.3 Update source and generated docs/runbooks so the canonical xUnit loop and prerequisites match the shipped behavior

## 3. Verification

- [x] 3.1 Add or update smoke/QA coverage for xUnit contour shape, generated starter surface, and runtime/doc truth
- [x] 3.2 Run the relevant OpenSpec, QA, and smoke verification set
- [x] 3.3 Update task/spec traceability so it reflects the final shipped state
