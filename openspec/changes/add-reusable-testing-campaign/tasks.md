## 1. Контракт кампании

- [ ] 1.1 Добавить `scripts/test/testing-campaign.sh` с командами `init`, `next`, `set` и `status`, проверкой входных данных и атомарной заменой `queue.jsonl`.
- [ ] 1.2 Добавить русскоязычную инструкцию формата кампании, состояний, приоритета очереди и продолжения работы человеком или агентом.
- [ ] 1.3 Добавить в инструкцию минимальные строки очереди и команды запуска для Vanessa BDD (`featurePath`) и YAxUnit (`filters`), включая их prerequisites.

## 2. Поверхность создаваемого проекта

- [ ] 2.1 Добавить в skeleton создаваемого проекта пустую структуру `analysis/testing/` без активной кампании, очередей и результатов.
- [ ] 2.2 Обновить generated-project guidance и overlay-манифест так, чтобы сценарий и документация распространялись, а проектное состояние кампаний не перезаписывалось при обновлении.

## 3. Проверка

- [ ] 3.1 Добавить один smoke-сценарий для создания кампании, приоритета `failed_retry → running → pending`, смены состояния, ошибочного обновления, исчерпания очереди и проверки селекторов Vanessa BDD/YAxUnit.
- [ ] 3.2 Подключить smoke-сценарий к `make agent-verify` и проверить bootstrap/overlay-контракт состояния кампаний.
- [ ] 3.3 Запустить `openspec validate add-reusable-testing-campaign --strict`, `make agent-verify` и `make export-context-check`.
