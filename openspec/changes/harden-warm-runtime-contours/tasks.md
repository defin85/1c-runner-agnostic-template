## 1. Contract

- [x] 1.1 Add OpenSpec deltas for `xpra` cleanup, warmed BDD event log checks, and PostgreSQL golden snapshots.

## 2. Implementation

- [x] 2.1 Harden `direct-platform` `xpra` session cleanup.
- [x] 2.2 Add warmed BDD run-complete marker, fixture-root substitution, and optional event log check.
- [x] 2.3 Add PostgreSQL golden `create`/`restore` scripts and Makefile targets.
- [x] 2.4 Update generated-project guidance/templates where command contracts changed.

## 3. Verification

- [x] 3.1 Update smoke contracts for `xpra`, BDD warm service, and golden baseline.
- [x] 3.2 Run OpenSpec validation and focused smoke tests.
- [x] 3.3 Run baseline template verification.
